# Cadence — Practice Studio (web)

Next.js frontend for the Practice Studio flow. See the repository root
[`README.md`](../README.md) for what's real, what's dev-mode, and how
to run it alongside the API.

```bash
npm install
npm run dev
```
